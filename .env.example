# Required: SAM.gov API Key
# Get your API key from: https://sam.gov/
SAM_GOV_API_KEY=your_sam_gov_api_key_here

# Optional: NAICS codes to track (comma-separated, max 50)
# Find your industry codes at: https://www.naics.com/search/
NAICS_CODES=541511,541512

# Optional: Collection interval in minutes (default: 60)
COLLECTION_INTERVAL=60

# Optional: Slack Webhook URL for notifications
# Get your webhook URL from: https://api.slack.com/messaging/webhooks
# Used when running with --notify flag to send alerts for new opportunities
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Optional: Pretorin CRM Integration
# Configure these to push opportunities directly to your CRM
# Get your API key from CRM Settings after logging in
CRM_URL=http://localhost:8000
CRM_API_KEY=crm_your_api_key_here